<resources>

    <style name="Theme.Intercorp.AlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert"/>

    <!-- Base application theme. -->
    <style name="Theme.Intercorp.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:displayOptions">disableHome</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/colorWhite</item>
        <item name="colorControlActivated">@color/colorAccent</item>
        <item name="alertDialogTheme">@style/Theme.Intercorp.AlertDialog</item>
        <item name="android:dialogTheme">@style/Theme.Intercorp.AlertDialog</item>
        <item name="android:alertDialogTheme">@style/Theme.Intercorp.AlertDialog</item>
        <item name="android:actionMenuTextColor">@color/colorPrimary</item>
    </style>

</resources>
